name: Comprobar Actions

on:
  push:
    branches:
      - fix/desglose-tomas-estado

permissions:
  contents: write

jobs:
  comprobar:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions-ok"
